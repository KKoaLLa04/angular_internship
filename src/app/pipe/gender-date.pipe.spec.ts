import { GenderDatePipe } from './gender-date.pipe';

describe('GenderDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderDatePipe();
    expect(pipe).toBeTruthy();
  });
});
